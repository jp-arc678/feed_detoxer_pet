<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.feeddetoxer">

    <!-- 1. สิทธิ์สำหรับการวาดหน้าจอทับ (ตาข่าย, จอฝืด, Guard Page) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- 2. สิทธิ์สำหรับการให้ Service รันอยู่ตลอดเวลาไม่โดนฆ่าทิ้งง่ายๆ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- 3. สิทธิ์สำหรับการแจ้งเตือน (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 4. สิทธิ์เข้าถึงสถิติการใช้งาน -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <!-- 5. ยกเว้นการปิดแอปอัตโนมัติจากระบบประหยัดแบตเตอรี่ (เพื่อให้ Brain Service ทำงานได้ตลอด) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 6. รับ Event เมื่อเครื่องเปิด (สำหรับ WorkManager watchdog ใน Phase 3) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 7. ให้แสดงแอปที่ติดตั้งทั้งหมดในหน้าเลือกแอป (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:label="Feed Detoxer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ============================================================== -->
        <!-- Service สำหรับ Overlay (ที่มาจาก flutter_overlay_window)     -->
        <!-- ============================================================== -->
        <service 
            android:name="flutter.overlay.window.flutter_overlay_window.OverlayService" 
            android:exported="false" 
            android:foregroundServiceType="specialUse">
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" 
                android:value="screen_filter_for_digital_wellbeing"/>
        </service>

        <!-- ============================================================== -->
        <!-- Brain Service — foreground service, UsageStats poller, session state machine -->
        <!-- ============================================================== -->
        <service
            android:name=".service.BrainService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="screen_time_monitoring" />
        </service>

        <!-- ============================================================== -->
        <!-- Boot receiver — restarts Brain after device reboot             -->
        <!-- ============================================================== -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>