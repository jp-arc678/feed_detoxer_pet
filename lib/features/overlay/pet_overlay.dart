import 'package:flutter/material.dart';

// Stub — full implementation in Phase 7.
// Will be: stateless overlay widget driven by Brain events, escalating pet mood.
class PetOverlay extends StatelessWidget {
  const PetOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pet Overlay — Phase 7'),
      ),
    );
  }
}
