import 'package:flutter/material.dart';

// Stub — full implementation in Phase 7.
// Will be: 10-second skippable guard page shown when a target app is opened.
class GuardPage extends StatelessWidget {
  const GuardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Guard Page — Phase 7'),
      ),
    );
  }
}
